# ESM 系列精读：从"会读蛋白"到"模拟进化"（ESM-1 → ESM-2 → ESM-3）

> 本文档精读三篇论文：**ESM-1b**（Rives 等，*PNAS* 2021，"Biological structure and function emerge from scaling unsupervised learning to 250 million protein sequences"）、**ESM-2**（Lin 等，*Science* 2023，"Evolutionary-scale prediction of atomic-level protein structure with a language model"）、**ESM-3**（Hayes 等，2024 预印本，后发表于 *Science* 2025，"Simulating 500 million years of evolution with a language model"）。
>
> 本文档**显式执行你的 8 条论文通读方法论**：每篇论文都以"五个问题"开局，单列"反直觉决策"小节，给"公式→直觉对照表"，把每张图当成"排除替代解释的实验"来读，放进技术史坐标，最后以"如果我是审稿人"的质询清单收尾——并且全程带着你的研究问题（功能距离与功效分析的稳定估计）做翻译。
>
> 三篇全文均来自 PubMed Central / bioRxiv 原文（见文末来源），所有数字可在原文核对。写法沿用你的标准：完整句子、加粗重点、每节"一句话记住"、不放英文原文摘录。

## 第 0 章 全景：三代 ESM 一张表 + 五问题速览

| | ESM-1b（PNAS 2021） | ESM-2（Science 2023） | ESM-3（Science 2025） |
|---|---|---|---|
| 规模 | 6.5 亿参数 | 家族 800 万–**150 亿** | **980 亿**（另有 14 亿、70 亿） |
| 数据 | 2.5 亿条蛋白序列（86 亿氨基酸，UniParc） | UniRef50 + UniRef90 采样增强（6000 万+ 序列） | **27.8 亿天然蛋白** + 合成增强 = 31.5 亿序列、2.36 亿结构、**7710 亿 token** |
| 模态 | 只有序列 | 只有序列 | **序列 + 结构 + 功能三模态** |
| 训练目标 | 掩码语言模型（填空） | 掩码语言模型 | **生成式掩码语言模型**（任意掩码率、任意顺序生成） |
| 核心发现 | 生物学性质从无监督学习中**涌现**（生化性质、远源同源、二级结构、接触、突变效应） | 结构信息随规模**涌现**，ESMFold 单序列预测结构逼近 AlphaFold2 | 三模态**提示词编程** + 偏好对齐 + 生成 **esmGFP**（与最近已知荧光蛋白 58% 一致） |
| 标志性数字 | 二级结构 71.6%（CB513） | ESMFold TM-score 82.8（CAMEO） | esmGFP ≈ 等效 **5 亿年**进化距离 |
| 一句话定位 | "证明可行" | "规模兑现" | "设计兑现" |

**五问题速览**（每篇论文的答案先看一遍，正文里再展开证据）：

| 五问题 | ESM-1b | ESM-2 | ESM-3 |
|---|---|---|---|
| 解决什么问题？ | 不用任何标注，只从序列统计里能不能学到蛋白的结构与功能信息？ | 不靠多序列比对（MSA），单条序列能不能做到原子级结构预测？ | 能不能生成**远离已知蛋白**、但真有功能的蛋白？ |
| 为什么以前解决不了？ | 老方法要么依赖 MSA（每个家族单独建模），要么监督学习要大量标注；无监督模型规模太小 | AlphaFold2 路线需要 MSA 搜索（慢、依赖同源物），且"单序列输入"时性能暴跌 | 已有生成模型要么只做序列（不知结构约束）、要么只做结构（扩散模型、不能组合提示词） |
| 方法核心创新？ | **把 BERT 式掩码语言模型直接搬到蛋白序列，并证明"规模"本身就是方法** | **注意力图 → 接触图**的免费读出 + 用语言模型表征替换 MSA 分支的 ESMFold 架构 | **把结构压成离散 token**（三模态统一成"语言"），任意掩码率训练 → 任意组合提示词生成 |
| 证据是什么？ | 线性投影/下游任务全线上分 + 与 ECE 的线性关系（scaling 证据） | 时序留出测试集（CAMEO/CASP14）+ 百万级宏基因组结构预测 | **湿实验**：合成 esmGFP，真荧光；偏好对齐后 Pass@128 翻倍 |
| 承认的局限？ | 接触预测仍不如 RaptorX 特征；最大模型仍欠拟合 | 与带完整 MSA 的 AlphaFold2 仍有差距；对低困惑度序列才最可靠 | 生成不保证功能（需筛选+实验）；序列之外仍有大量生物学未建模 |

**一句话记住**：三代 ESM 讲的是同一个故事的三段——**"蛋白序列是进化写的语言，语言模型规模越大，能从中读出的生物学越多"**；第一代证明这个命题成立，第二代把它兑现成"不需要 MSA 的结构预测"，第三代把它兑现成"能按提示词设计全新蛋白"。

## 第 1 章 ESM-1b 精读（PNAS 2021）

### 1.1 五个问题

**它解决什么问题？** 生物学有个老命题：蛋白的结构和功能"记录"在进化的序列统计里——自然选择筛掉的序列变异，留下了关于哪个位置重要、哪些位置协同的痕迹。问题是：**能不能不用任何标注、不查任何数据库，纯靠"读大量序列"把这些信息自动提取出来？** ESM-1b 的回答是能——只要把"填空"式语言模型训练得足够大。

**为什么以前解决不了？** 因为当时的两条技术路线各有死穴：直接耦合分析（DCA）这类方法**每个蛋白家族都要单独建模型、单独做多序列比对**，贵且慢，而且学不到跨家族的共性；而监督学习需要人工标注（结构、功能标签），能用的数据少两个数量级。此前的蛋白语言模型（UniRep、TAPE 时代的 LSTM）规模都在千万到一亿参数以下，没人知道"规模"本身会不会带来质变。

**方法的核心创新是什么？** 其实方法上全是"拿来主义"——把自然语言处理里的 **BERT 式双向 Transformer + 掩码语言模型目标**原样搬到蛋白序列上，词表换成 20 种氨基酸。真正的贡献是**用实验证明了"规模即方法"**：在 2.5 亿条序列上把模型做到 6.5 亿参数后，结构、功能、进化关系的信息**自动涌现**在表征里——不需要任何生物学的先验设计。

**用什么证据证明有效？** 一整套"拷问表征"的实验（1.3 节详解）：从最浅的生化性质聚类，到同源蛋白聚类、远源同源检测、家族内隐含比对，再到线性投影读出二级结构和接触图，最后到监督微调打平当时的突变效应预测冠军。**贯穿始终的一条最强证据是那个"线性关系"**：语言建模的损失（ECE）和表征里的结构信息含量严格线性相关——说明"模型越懂序列语言，就自动越懂结构"。

**作者承认的局限是什么？** 接触预测（50.2）仍然打不过当时最强的 RaptorX 特征（59.4）——单靠无监督表征还差一口气；最大的 6.5 亿参数模型**仍然欠拟合**（数据太多、容量不够），作者明确预测"继续加规模会更好"——这句话直接就是 ESM-2 的立项书；另外作者建议未来加入宏基因组序列。

### 1.2 反直觉决策（这篇论文的精华所在）

**反直觉决策一：用"填空"（掩码语言模型），而不是当时更流行的"顺句生成"（自回归）。** 2019 年 GPT 系已经证明了自回归的威力，但作者选了 BERT 式的双向填空。为什么？因为这篇文章要的是**表征**而不是生成器：填空迫使模型同时利用左右两侧的上下文，每个位置的表征都浸润了全序列的信息——而自回归只能看左半边。回头看这个选择极其关键：ESM 系后来的所有"结构信息涌现"（包括 ESM-2 的接触图）都建立在双向表征之上。（代价是：掩码模型算整条序列的似然很别扭，后来的变异效应打分只能靠"伪似然"凑合。）

**反直觉决策二：什么生物学先验都不加，连"结构"两个字都没让模型知道。** 当时的主流是把领域知识硬编码进模型（接触约束、二级结构监督等）。作者反其道而行：**只喂原始氨基酸序列，其余一概不教**，然后用线性投影去"审讯"表征里有没有自发长出结构信息。为什么这个选择是精华？因为只有这样才能回答"结构信息到底是无监督学习自己学到的，还是我们塞进去的"——**放弃先验，才能证明涌现**。

**反直觉决策三：把"数据多样性"当成一个可调的超参数来研究。** 作者专门构造了三个数据集：低多样性的 UR100、高多样性稀疏的 UR50/S、高多样性稠密的 UR50/D（UniRef 聚类后的代表序列，UniRef 数字越小去重越狠、多样性越高）。结果发现**多样性比数量更值钱**：高多样性数据集上的模型泛化明显更好，且对"接触预测"这类需要跨家族共性的任务提升最大。这个发现后来成了整个领域的数据配比准则。

**反直觉决策四（隐藏款）：用"语言建模损失"当结构信息的代理指标。** 别人调模型看下游任务分数，这篇论文发现**只看语言建模的 ECE 就够了**——ECE 和结构信息含量严格线性相关。这意味着后续研究可以完全不碰昂贵的结构评估，只看训练损失就能判断"这个模型大概懂多少结构"。这是"scaling laws 在生物序列上成立"的第一个证据，也是 ESM-2 敢直接按规模设计实验的原因。

### 1.3 公式 → 直觉对照表

| 公式/方法 | 一句话人话 |
|---|---|
| 掩码语言模型损失 L = −Σ log p(xᵢ \| x\m) | 把序列里 15% 的位置盖住让模型猜——**"藏几个字，逼它靠上下文猜回来"**；猜得准，说明它懂了全局约束 |
| ECE（指数化交叉熵） | **"平均每个位置它还在几个氨基酸之间犹豫"**——1 分是神，25 分是瞎猜。8.46 意味着每个位置平均只纠结 8 个选项 |
| 线性投影（逻辑回归读二级结构；位置对点积读接触） | **"拿一把尺子去量表征"**——如果一把线性尺子就能从表征里读出结构，说明结构信息是明晃晃摆在那里的，不是藏在非线性深处 |
| 位置对点积 | 位置 i 和 j 的表征越"对齐"（内积越大），它们在三维空间里越可能挨着——**"表征里的默契 = 结构里的接触"** |
| t-SNE / PCA 降维 | 把高维表征压到二维平面看聚类——**"给表征拍张俯瞰照"** |
| 余弦相似度分布（对齐 vs 非对齐残基对） | 同家族蛋白里"应该对齐"的两个位置，表征相似度显著高于随机配对——**"模型心里有一份隐式的多序列比对"** |
| 伪似然（掩码模型算整条序列概率的近似） | 把整条序列的概率拆成"每个位置的填空概率之积"——掩码模型的"整句打分"替代品（你的变异效应打分会用到它） |

### 1.4 把图当实验设计读（每张图在排除什么替代解释）

**图 1（t-SNE：输出嵌入的氨基酸聚类）**——排除的解释是"模型只是记住了氨基酸频率"。如果只靠频率，嵌入里不会出现"疏水/亲水/芳香族"这种**生化性质**的聚类结构。聚出来了，说明模型学到了化学课本第一课。

**图 2（同源基因聚类 + PCA 抽出物种轴与直系同源轴）**——排除的解释是"聚类只是序列相似性的平凡结果"。妙在设计：PCA 抽出的是**线性**的两个方向（物种轴、基因轴），说明"进化关系"以干净的低维结构存在于表征里，而且**未训练模型和频率基线都没有**这个结构——是学习过程的产物，不是数据自带的。

**图 3（家族内对齐 vs 非对齐残基对的余弦相似度分布分离）**——排除的解释是"模型只是把相同的氨基酸映射到相同的位置"。实验专门做了控制：**只比较氨基酸种类相同的残基对**，分离依然存在。这说明模型学到的是**位置级的家族对应关系**（隐式 MSA），而不是化学性质的同义替换。

**图 4/5（线性投影读出的二级结构和接触图，与 CCMpred 对比）**——排除的解释是"结构信息来自我们塞进去的监督"。因为用的是**线性**投影 + **时序/家族留出**交叉验证（按 family/superfamily/fold 三个层次切分），投影方式又简单到不可能"伪造"信息。接触图还赢过了 CCMpred（传统共进化分析）。

**图 6（ECE 与结构信息含量的线性关系）**——排除的解释是"下游性能好只是运气或过拟合"。这条线把"语言建模质量"和"结构知识"钉死在一条直线上，**从机制上**把二者绑定：不是巧合，是同一个学习过程的两面。

**图 7（突变效应：微调 Transformer 打平 Envision）**——排除的解释是"需要结构和进化特征才能预测突变效应"。Envision 显式用了结构+进化特征，而 ESM-1b 微调**只吃原始序列**就打平了它——说明进化信息已经内化在表征里。

### 1.5 技术史坐标

**之前是谁**：UniRep（2019，用 LSTM 做蛋白表征）、TAPE（2019，统一基准）、以及同期的几个小规模蛋白语言模型（Alley、Heinzinger 的 LSTM、Rao 的 38M Transformer）。ESM-1b 直接把这些小模型按在地上：同参数量级下 Transformer 全面压过 LSTM（论文表 8 的直接对比）。

**之后是谁**：ESM-2 把"规模"这个旋钮拧到 15B 并收获了结构预测；ESMFold 成了单序列结构预测的代名词；同时 ESM-1b 的**嵌入**成了整个蛋白工程界的通用特征（直到今天你的很多下游工具里 `esm1b_t33_650M` 还是默认选项）。它的"伪似然打分变异"更是成了后续所有蛋白语言模型变异效应预测的标准姿势。

**一句话记住**：ESM-1b 在历史上的位置是**"第一张完全兑现的支票"**——它证明了分布语义学在蛋白序列上成立、规模是唯一需要的方法、且"语言建模损失"本身就是生物学信息的可靠读数；这三条结论直接规定了 ESM 系此后所有论文的实验设计。

### 1.6 如果我是审稿人，我会问什么

1. 线性投影读出的结构信息，会不会主要来自"训练集和测试集之间的家族重叠"？——论文用 family/superfamily/fold 三层留出交叉验证回应了，且 fold 层几乎没有性能衰减（对二级结构而言），这个辩护是站得住的。
2. 接触预测只有 50.2、输给 RaptorX 特征 59.4——"状态最先进"的说法是不是过誉？——论文承认差距，但"无监督特征 + 现有特征 = 超越现有特征"（组合后 +3.9%）这条线其实更有价值：**它不替代 SOTA，它喂养 SOTA**。
3. 6.5 亿参数的模型仍欠拟合，为什么不再做大？——算力预算使然；这个"遗憾"是 ESM-2 的动机。
4. 突变效应预测只测了两个数据集，够吗？——按当时标准够了，且后续 ESM-1v 专门补了零样本突变效应的大规模评测（这是留给你的线索：**ESM-1v 的零样本变异打分与你 PhyloPower 的效应量先验直接相关**）。

### 1.7 带着你的问题读：这一篇对你有什么用

- **ESM-1b 嵌入 = 你功能距离问题的现成第三参照系**。PhyloFunc 与 UniRef 距离之争的僵局，可以引入 ESM 嵌入余弦距离做仲裁：它是**从纯序列统计里长出来的功能相似度**，与系统发育树完全独立。你之前发现的"真实 within d_eff≈9.1 vs PhyloFunc 全维≈13"，用 ESM 嵌入空间的 top-3 PCoA 截断做交叉验证，能判断低秩结构是功能流形的本质还是度量伪影。
- **伪似然变异打分 = 蛋白侧变异效应的零成本先验**。宏蛋白组菌株间差异位点，可以用 ESM-1b 伪似然比批量打分，作为功效分析里"效应量"的确定性先验——与 Evo 2 的 DNA 似然比互补（一个看蛋白层、一个看 DNA 层）。

## 第 2 章 ESM-2 精读（Science 2023）

### 2.1 五个问题

**它解决什么问题？** 结构预测。当时 AlphaFold2 刚封神（2021），但它的路线有一个结构性成本：**必须先在数据库里搜出进化同源序列、建多序列比对（MSA）**——单条蛋白序列直接喂进去，性能断崖式下跌。ESM-2 问的是：**把蛋白语言模型放大到 150 亿参数，"结构"会不会像学外语一样被模型自己学会——从而完全不需要 MSA？**

**为什么以前解决不了？** 两条原因。第一，没有人把蛋白语言模型做到过这个规模——此前最大的（就是自家 ESM-1b）才 6.5 亿参数，而"结构从规模中涌现"需要跨过某个未知阈值；第二，MSA 提供的信息（同源序列的共进化信号）被认为不可替代，而语言模型恰恰要证明**它在训练时"见过"的千百万同源序列，已经把共进化信号内化了**——这个命题在当时没有证据。

**方法的核心创新是什么？** 两个创新，一大一小。小的：架构改进（相对位置编码 RoPE 替代绝对位置编码，摆脱了 ESM-1b 的 1024 长度上限）。大的：**ESMFold 架构**——把 AlphaFold2 里最昂贵、最耗时的"MSA 处理模块"整个拆掉，换成"蛋白语言模型表征直接进折叠主干"（一个简化成单序列版的 Evoformer + AlphaFold2 的结构模块）。整条流水线变成"序列 → 语言模型 → 折叠网络 → 三维坐标"，端到端、全 GPU、无数据库搜索。

**用什么证据证明有效？** 四层证据：① **scaling 曲线**——从 800 万到 150 亿参数，接触预测和结构预测单调上升，且出现**非线性的能力跳跃**（个别蛋白在跨过某个规模阈值时预测质量突然变好，比如 7QQA 在 150M 处、T1056 在 15B 处）；② **时序留出的硬核评测**——CAMEO 和 CASP14 测试集都在训练数据截止日期之后，ESMFold 拿到 TM-score 82.8（CAMEO）和 67.8（CASP14），全面碾压"单序列版"的 AlphaFold2 和 RoseTTAFold，且与"带完整 MSA 的 RoseTTAFold"打平；③ **消融实验**——拆掉语言模型，性能从 0.74 掉到 0.58 lDDT：**语言模型是整条流水线最大的贡献者**；④ **规模-理解的一致性**——困惑度（模型对序列的熟悉程度）与结构预测 TM-score 强相关（-0.55/-0.67），而且**在 15B 全家族里，验证集困惑度与 TM-score 的相关系数高达 -0.99/-1.00**——"越懂序列，越懂结构"，机制上闭环。

**作者承认的局限是什么？** 与带完整 MSA 的 AlphaFold2（88.3/84.7）仍有明确差距——单序列路线目前的天花板；性能对"低困惑度序列"（模型熟悉的、进化上资料丰富的蛋白）最好，对"模型不熟的孤儿序列"退化明显；另外 ESMFold 训练时用了 **1200 万个 AlphaFold2 预测结构做蒸馏**（75% 采样自预测结构）——这是一个诚实的依赖，也意味着它间接继承了 AlphaFold2 的知识。

### 2.2 反直觉决策

**反直觉决策一：不跟 AlphaFold2 正面刚，而是把它的"命根子"（MSA）整个扔掉。** 2022 年的空气里全是 MSA，而这篇论文赌的是"**语言模型在预训练时已经吃掉了同源信息，MSA 的活儿它内化了**"。这个赌注的逻辑链极其漂亮：MSA 的本质是"汇总同源序列的共进化统计"，而语言模型正是在千百万同源序列上训练的——**为什么要在推理时重新搜一遍训练时已经消化的东西？** 收益是革命性的：预测速度比 AlphaFold2 快一到两个数量级（384 残基蛋白单卡 V100 上 14.2 秒），而且**彻底摆脱了"没同源物就测不准"的场景**——孤儿蛋白、宏基因组蛋白、从头设计的蛋白。

**反直觉决策二：用"注意力图"当接触图读，分文不花。** 结构预测通常要专门训练一个输出头，而这篇论文借用了 Rao 等 2021 的发现：**注意力模式本身就是接触图**——把各层各头的注意力图对称化、做 APC 校正后，喂一个只用 20 个蛋白训练的逻辑回归，就能当接触预测器用。这提供了一个**零成本的"结构知识温度计"**：不用为每个模型规模都训练一个折叠头，看注意力图就知道这个模型"懂多少结构"——scaling 实验因此能以极小代价跑完全家谱。

**反直觉决策三：从训练集里"剔除了所有从头设计的蛋白"。** 预训练数据里 1,027 条标注"人工序列"的、以及围绕 81 个策展的 de novo 蛋白的 58,462 条同源序列，全部删除。为什么自断数据？**为了给"泛化到自然进化之外"留一个干净的评测场**——这为后来的 ESM-3（生成"自然中不存在"的蛋白）埋下了伏笔：模型对 de novo 序列的一切能力，都来自它对自然序列规律的归纳，而不是记忆。

**反直觉决策四：蒸馏 AlphaFold2 的预测结构当训练数据（75% 采样权重）。** 用"对手"的预测产物训练自己，看起来像作弊，实际是当时的标准做法（自蒸馏/伪标签）：结构数据的稀缺瓶颈（PDB 实验结构只有十几万条）靠 AlphaFold2 的 1200 万条预测结构补上。作者诚实报告了这个依赖，而消融显示去掉蒸馏数据性能下降——**"站在对手肩膀上"被坦率地写成了方法的一部分**。

### 2.3 公式 → 直觉对照表

| 公式/方法 | 一句话人话 |
|---|---|
| 困惑度（perplexity）= exp(−log p(x)） | **"平均每个位置在几个氨基酸之间犹豫"**——蛋白语言模型里 1 是完美、20 是瞎猜。15B 模型 6.37，意味着每个位置平均只纠结 6 个选项 |
| 注意力图 → 接触图（对称化 + APC 校正 + 逻辑回归） | **"谁在看谁 = 谁挨着谁"**——模型为了填空而建立的"位置间关注"，正好是三维接触的投影 |
| TM-score / lDDT / pLDDT | 结构相似度（0–1，>0.5 同一折叠）/ 逐残基距离准确性 / 模型对自己预测的置信度打分 |
| 结构模块（structure module，取自 AlphaFold2） | 把"成对表征"翻译成三维坐标的等变网络——**"从关系矩阵到空间摆位"** |
| 蒸馏（distillation） | 用 AlphaFold2 预测的结构当"标准答案"训练——**"让老师傅的作业本成为教材"** |
| 时序留出（temporal holdout） | 测试集全部来自训练截止日期之后解出的结构——**"考前绝没见过真题"** |
| 单序列 vs MSA 路线 | MSA 路线 = 先花 30 分钟在数据库里找亲戚；单序列路线 = 亲戚信息已经"长在"模型参数里 |

### 2.4 把图当实验设计读

**图 1A（训练曲线：全家族困惑度随训练步数下降）**——排除的解释是"15B 太大训不动/不收敛"。曲线干净收敛，且大模型每一步的困惑度都更低：规模红利是实打实的。

**图 1B–D（无监督接触预测随规模的变化）**——排除的解释是"大模型只是把训练集背得更熟"。关键设计是**按"训练集中同源序列数量"分桶**（MMseqs 命中数）：结果显示**进化资料越少的序列，大模型的相对优势越大**——也就是说大模型的增益恰恰集中在"没得抄"的孤儿蛋白上，这直接反驳了"背诵"假设，证明是**跨家族的规律归纳**。图 1D 还展示了"困惑度降幅大的蛋白 = 接触预测涨幅大的蛋白"——两个信号同源。

**图 1E–F（全原子结构预测随规模的非线性跳跃）**——排除的解释是"结构预测只是随规模平滑变好"。作者专门挑了 7QQA 和 T1056 两个例子：前者的 RMSD 在 35M→150M 时从 7.0 跳到 3.2，后者在 3B→15B 时从 4.0 跳到 2.6，**跳变前后都近乎平台**——这是"涌现"的教科书级展示：能力不是挤出来的，是跨过阈值后"点亮"的。

**图 2B（消融：去掉语言模型 / 去掉折叠主干 / 缩小主干）**——排除的解释是"折叠网络才是主力，语言模型只是点缀"。结果相反：**去语言模型掉到 0.58 lDDT，去折叠主干只掉到 0.66**——语言模型是最大的贡献者。这张图定义了 ESMFold 的"功劳归属"。

**图 2C（散点图：按困惑度上色，低困惑度点与 AlphaFold2 几乎重合）**——排除的解释是"ESMFold 只是 AF2 的弱化版"。图说得很细：**模型熟悉的蛋白，它和 AlphaFold2 一样准；差距集中在模型不熟的蛋白上**——这同时给出了"什么时候该信它"的操作指南（先算困惑度，再决定信不信——与你 Evo 精读里 fig S8B 的教训一模一样）。

**图 3（100 万宏基因组蛋白的结构预测）**——排除的解释是"这只是一次炫技"。它的科学主张是**规模效应**：ESMFold 用约 1600 GPU 小时、不到一天算出 100 万个结构（29% 高置信），其中 7,271 个与已知结构库都不相似、317 个连序列带结构都是全新的——**"结构先行"为功能注释开路的路线**在这里被实打实跑通（文中两个例子：序列上搜不到任何匹配的蛋白，结构上却是清晰的核酸酶和脂质结合域）。

### 2.5 技术史坐标

**之前是谁**：ESM-1b（同一团队，证明了规模方向）；AlphaFold2/RoseTTAFold（MSA 路线的双雄）；Rao 2021（注意力=接触图的发现者）。

**之后是谁**：ESMFold 直接引爆了"大规模结构预测"运动——Meta 随后发布了 6 亿蛋白的 ESM Metagenomic Atlas（宏基因组结构图谱）；无数论文用 ESMFold 给孤儿蛋白做功能注释；EvolutionaryScale 2025 年的 **ESM C / ESMFold2** 是这条线的直系后代（单序列结构预测 0.85 lDDT，抗体-抗原复合物 DockQ 50% 反超 AlphaFold3 的 47%）。而 ESM-2 的"150M 小模型打赢 ESM-1b 650M"还顺手确立了**参数效率**这个后来被 ESM C 发扬光大的主题。

**一句话记住**：ESM-2 的历史地位是**"用规模把语言模型推成了结构预测器"**——它证明蛋白语言模型不需要 MSA 也能逼近原子级精度，代价是牺牲一点极限精度、换来一到两个数量级的速度和"无同源物也能测"的自由度；"困惑度=可信度"的诊断工具也是它留给后人的礼物。

### 2.6 如果我是审稿人，我会问什么

1. 你们声称"单序列预测"，但训练时蒸馏了 1200 万 AlphaFold2 结构——这不就间接用了 MSA 信息吗？——诚实回答：推理时确实不需要 MSA，但训练时继承了 AF2 的知识；消融显示去掉蒸馏会掉分。这是"蒸馏算不算作弊"的开放争论，论文把选择权交给了读者。
2. ESMFold 在 CASP14 上 TM-score 67.8 对 AF2 的 84.7，差距不小——"competitive"是否措辞过强？——论文的说法是"与带完整 MSA 的 RoseTTAFold 打平（82.0 CAMEO）"以及"低困惑度序列上与 AF2 相当"；定位是"更快的替代品"而非"更强的冠军"，这个定位是诚实的。
3. 结构预测的"涌现"会不会只是更大模型记住了更多训练结构？——图 1B/C 的按同源深度分桶实验是对此最有力的回应（孤儿序列受益最大）。
4. 你们自己剔除了 de novo 蛋白——那 ESMFold 对从头设计蛋白的预测可靠吗？——论文未答，但这是留给 ESM-3 的问题（答案是有条件可靠）。

### 2.7 带着你的问题读：这一篇对你有什么用

- **"困惑度 = 可信度"的规则直接可用于你的蛋白 pilot 数据**：用 ESM-2（或更省的 ESM C 600M）给你 PXD069517 里的蛋白序列算困惑度，按困惑度分层检验功能距离度量的表现——如果高困惑度（模型不熟）蛋白的功能距离度量更不稳定，你就找到了 pilot 曲线不重叠的一个**确定性解释变量**。
- **ESMFold 是宏蛋白组功能注释的利器**：你的 CRC 数据集里"序列搜不到同源物"的孤儿蛋白，可以批量折叠、用 FoldSeek 做结构搜索补功能注释——在功效分析里把"未注释"从缺失值变成可用信息。
- **ESM-2 的注意力接触图**给你一个有趣的研究问题：**用接触图的相似性定义"结构约束相似度"**，作为与 PhyloFunc、UniRef、嵌入距离并列的第四种功能距离——看哪一种在 pilot 功效估计上最稳。

## 第 3 章 ESM-3 精读（Science 2025）

### 3.1 五个问题

**它解决什么问题？** 把"读懂蛋白"升级为"**按需设计蛋白**"。具体要证明的命题相当激进：**一个生成模型能不能产出"远离一切已知蛋白"、却真实有功能的蛋白？** 终极考卷选了荧光蛋白（GFP）——因为荧光机制苛刻到"发色团必须由蛋白自身的原子自发催化形成"，自然界里独一无二，几个随机突变就能把荧光清零，而且它被人类定向改造了几十年，想"抄作业"都无处可抄。

**为什么以前解决不了？** 三个前置障碍。第一，**多模态无法统一**：序列模型（如 ESM-2）能生成序列但不懂结构约束，结构生成模型（如 RFdiffusion、Chroma）用三维扩散但**接不住"功能"这类抽象提示词**——序列、结构、功能三张皮合不到一张上；第二，**可控性弱**：无条件生成的蛋白大多没功能，而"提示词编程"当时在蛋白领域没有先例；第三，**规模**：要支撑三模态的泛化能力，需要百亿参数级别和万亿 token 的训练量——这需要一个新公司（EvolutionaryScale，Arc 系创业公司）的算力预算。

**方法的核心创新是什么？** 一句话：**把一切都 token 化**。三维结构不是用扩散模型在欧氏空间里生成，而是**训练一个离散自编码器把每个残基的局部结构压成一个离散 token**（重构误差 <0.5Å），于是序列、结构、功能关键词三条轨道变成三串"同一种语言"的 token，喂进同一个 980 亿参数的双向 Transformer。训练目标是**生成式掩码语言模型**：训练时用**任意掩码率**（从几乎全掩到几乎不掩）盖掉任意组合的轨道——这带来一个匪夷所思的性质：**生成时可以从任意位置、以任意顺序、从任意轨道开始补全**。配合第一层里的几何注意力（直接吃骨架原子坐标做条件），模型既能接"原子级提示词"（某个结合位点的精确坐标）也能接"抽象提示词"（"我要一个蛋白酶"的功能关键词），还能把两者**自由组合**。

**用什么证据证明有效？** 四层证据，一层比一层硬：① **提示词忠实度**——四种提示词（坐标、二级结构、溶剂可及性、功能关键词）各自独立评测，7B 模型即可稳定跟随，还能组合"原子级基序 + 抽象指令"解出自然界里找不到对应蛋白的骨架；② **偏好对齐**——用"好样本 vs 差样本"偏好对做偏好优化（DPO 式），对齐后 98B 模型在三级接触基序脚手架任务上 Pass@128 从 26.8% 翻到 **65.5%**（小模型 9.5%→18.8%），且规模越大对齐收益越大——**"潜伏能力被对齐唤醒"**；③ **单序列结构预测反超 ESMFold**（0.880 vs 0.861 平均 lDDT，CAMEO）——顺带把上一代的纪录破了；④ **终极湿实验：esmGFP**——用"链式思考"流程（先生成结构 token 当骨架、再生成序列、再交替优化、每步过滤活性中心原子协调性）生成候选，两轮 96 孔板实验，第一轮在 B8 井找到一个"半亮"设计（与已知荧光蛋白 57% 一致、亮度只有天然 GFP 的 1/50、发色团要一周才成熟），**以它为起点续写第二轮**，C10 井的设计——**esmGFP**——亮度进入天然 GFP 的分布范围，与最近已知荧光蛋白（tagRFP，还是个人工设计变体）**58% 序列一致**（96 个突变，22 个在蛋白内部），与最近的天然荧光蛋白 53%。用珊瑚类 GFP 的"时间校准系统发育"做标尺，58% 的差异对应自然界里**超过 5 亿年的进化时间**。对照实验：把发色团关键残基 Thr65/Tyr66 突变掉，荧光消失——**证明荧光确实来自设计的那套机制**。

**作者承认的局限是什么？** 讨论里最诚实的一句是：**生成绝不等于功能**——自回归/掩码生成都会"幻觉"出看起来合理但没功能的蛋白，所以一切设计都绕不开计算筛选 + 实验验证；序列之外的生物学（翻译后修饰、动态、环境依赖）仍然不在模型视野内；另外 ESM-3 的完整权重并未开源（开源的是学术版 ESM3-open + API），可复现性打了折扣。

### 3.2 反直觉决策

**反直觉决策一：把三维结构"压扁"成离散 token，而不是像 RFdiffusion/Chroma 那样在连续三维空间里做扩散。** 这是全篇最大胆的架构选择。常识是"结构是连续几何，应该用几何网络处理"；ESM-3 反其道而行：**先让一个自编码器把结构压成 token，然后纯用语言模型玩三模态的组合**。为什么值得？因为"离散 token + 全对全注意力"是**唯一能自由混合三条轨道、任意顺序生成**的框架——扩散模型生成 3D 坐标，怎么接"功能关键词"这个 token？怎么让"先给定功能、再填结构、再填序列"的链式思考成为可能？**为了多模态的可组合性，牺牲了几何的连续性**——事实证明这笔交易极其划算（重构误差 <0.5Å 说明几何信息基本没丢）。

**反直觉决策二：训练时用"任意掩码率"的噪声调度，而不是固定的 15% 掩码。** 经典 BERT 固定掩 15%；ESM-3 让掩码率在训练中从低到高扫描。原因写在论文里：**高掩码率练出生成能力，低掩码率练出表征质量**——噪声调度把两种能力一起买下。副产品是"任意顺序生成"：模型见过所有掩码组合，所以推理时从哪个位置先填都行。

**反直觉决策三：把 NLP 的"偏好对齐"（DPO 那套）搬到蛋白设计。** 对齐在 LLM 里是让模型"更像人话"，在蛋白里是什么？答案是**"生成物对提示词的忠实度"**：用"高分样本（结构置信高、与提示词坐标吻合）vs 低分样本"构成偏好对，让模型学会"照着题目写答案"。结果惊人：**对齐唤醒了规模越大越深的潜伏能力**（98B 对齐后 65.5% vs 未对齐 26.8%），而且对齐只需要少量示例就能泛化——这是"预训练表征里藏着远超表面的能力"的直接证据，也是 NLP 路线图（预训练 → 对齐）在生物序列上第一次完整复现。

**反直觉决策四：用"链式思考"生成 GFP，而不是一次性生成。** 让模型先单独生成结构骨架（检查活性中心的原子协调性）→ 再加结构条件生成序列 → 再序列-结构交替优化——**把"设计一个发光蛋白"拆成多步可过滤的小任务**。这模仿的是人类设计者的工作流，也让每一小步都能被独立质控。第一轮的 B8 不够亮怎么办？**不放弃，把 B8 当新一轮的起点继续思考**——这就是"链"的含义，也是最终 esmGFP 出现的关键操作。

**反直觉决策五（叙事层面的）：把"58% 序列一致"翻译成"5 亿年进化"。** 用一个时间校准的珊瑚类系统发育树，把"荧光蛋白间序列一致性"校准成"分化时间"，然后宣称 esmGFP 等效于自然界 5 亿年的探索。这当然是修辞（模型不是沿进化路径走的），但论文在讨论里给了辩护：**语言模型预测的"下一个 token"是由进化生成的，所以学会预测 token = 学会进化能走的路径**——"进化模拟器"的隐喻不是广告词，而是他们对"语言建模任务本质"的理论立场。

### 3.3 公式 → 直觉对照表

| 公式/方法 | 一句话人话 |
|---|---|
| 结构 token 化（离散自编码器，几何损失监督距离与方向） | **"把每个残基的三维街景拍成一张数字快照"**——编码器拍、解码器还原（<0.5Å 误差），token 就是快照的编号 |
| 生成式掩码目标（任意掩码率噪声调度） | **"用各种比例的涂黑训练"**——全涂黑=从头生成，少涂几笔=补全，任何一个"从任意部分出发填满全局"的姿势模型都练过 |
| 三轨道融合（序列/结构/功能 token 进同一个 latent 空间） | **"三种语言翻译成同一种中间语言再合成一句话"** |
| 几何注意力（第一层，局部参考系 + 全局变换） | **"让模型直接摸到原子的坐标"**——每个残基在自己的坐标系里描述邻居，再变换到全局坐标系互相交流 |
| 偏好优化（好样本 vs 差样本） | **"对的答案多给糖，错的答案扣分"**——让模型学会把生成质量向"高分方向"搬 |
| Pass@128 | **"给 128 次尝试机会，能解出这道题的概率"**——衡量生成模型的"解题率"，对齐后 98B 达到 65.5% |
| 链式思考（chain of thought） | **"先画骨架、再填序列、再反复打磨"**——把一个大设计任务拆成可逐步质检的流水线 |
| 序列一致性 → 进化时间校准 | **"按自然界的速度表，58% 的序列差异 = 5 亿年"**——用珊瑚类荧光蛋白的化石级时间标尺换算 |

### 3.4 把图当实验设计读

**图 1D（三条轨道的负对数似然随训练算力下降）**——排除的解释是"三模态只是拼盘，训练互相干扰"。曲线显示三条轨道一起变好，且**条件生成与无条件生成的差距随规模拉大**——模型越来越"听得懂提示词"。

**图 1E（无条件生成的蛋白嵌入 UMAP，与 UniProt 天然序列重叠）**——排除的解释是"无条件生成只会吐训练集的近邻"。结果：生成物多样性高（平均两两序列一致性仅 0.155）、质量高（平均 pLDDT 0.84），且**覆盖了天然序列的分布**——模型学的是分布本身，不是记忆点。

**图 2B（分布外提示词：人工对称设计蛋白的二级结构提示）**——排除的解释是"模型只能生成它见过的折叠"。用**训练集里不存在的对称折叠**做提示，生成物序列一致性 <20%、结构新颖但仍高置信折叠——模型把"提示词语言"推广到了自然蛋白空间之外。

**图 2C（原子级基序 + 抽象指令的组合题）**——排除的解释是"解基序题只是在偷提示词的原始骨架"。关键数字：生成解与基序来源骨架的中位 TM-score 仅 0.36–0.40——**骨架是新的**；对血清素、钙离子、蛋白酶抑制剂、Mcl-1 抑制剂结合位点等基序，在已知结构库里**找不到任何含同样基序的蛋白**——即模型为这些基序发明了全新容器。

**图 2D（胰蛋白酶压缩 33%）**——排除的解释是"压缩只是删掉不重要区域"。活性中心三元组的全原子 RMSD 0.73Å——**砍掉三分之一的序列，酶的活性中心原子级原样保留**，而且折叠拓扑只靠一个功能关键词指定。这是"跨抽象层级提示词"的极限演示。

**图 3（对齐）**——排除的解释是"对齐只是过拟合评测集"。评测用 46 个配体结合基序、每个基序生成 1024 个不同排列/位置的提示词、Pass@128 无偏估计——对齐收益在全部三个规模上一致出现，且**对齐后的模型解出的不同方案数量也更多**（不止解题率高，解法还多样）。这排除了"只会背一种答案"。

**图 4（esmGFP）**——排除的解释是"荧光来自污染/发色团来自外源"。对照设计：无 GFP 序列的阴性对照、已知 GFP 阳性对照、以及**把 esmGFP 发色团关键残基突变成甘氨酸后荧光消失**——证明荧光正是由设计的 Thr65/Tyr66 机制产生的。光谱（激发峰 496nm、发射峰 512nm，与 EGFP 形状一致）进一步确认这是 GFP 家族的机制而非别的发光物质。

### 3.5 技术史坐标

**之前是谁**：自家 ESM-2（序列单模态的天花板）；结构生成系 RFdiffusion（贝克实验室，扩散模型做骨架设计）和 Chroma（Generate Biomedicines）；以及"功能条件"的早期尝试（ProGen、ESM-1v 的序列侧微调）。ESM-3 的定位是**把这三条河并成一条**。

**之后是谁**：ESM-3 之后蛋白基模分了两条线——**一条继续"表征回归"**：ESM C（2025）把数据扩到 28 亿序列、600M 参数打平 ESM-2 15B，ESMFold2 单序列结构预测 0.85 lDDT、抗体-抗原复合物预测反超 AlphaFold3；**另一条继续"生成与对齐"**：ESM-3 的偏好对齐成了蛋白生成的标准动作，链式思考/推理时引导被后来者（包括 Evo 2 的染色质设计）继承。而 esmGFP 的"进化时间"修辞也成了行业梗——每篇"生成全新蛋白"的论文现在都要回答"离已知蛋白多远"。

**一句话记住**：ESM-3 的历史地位是**"把蛋白语言模型从'读'推到了'写'，并且写出来的是湿实验验证过的真东西"**——它的三个方法学遗产（全 token 化的多模态统一、任意掩码率的生成式训练、偏好对齐唤醒潜伏能力）定义了 2024 之后蛋白生成模型的标准配方。

### 3.6 如果我是审稿人，我会问什么

1. "模拟 5 亿年进化"的换算是不是过度修辞？——基于"序列一致性-分化时间"的经验标尺有依据，但模型没有沿进化路径生成；论文讨论里用"进化模拟器"的理论立场（预测进化生成的 token = 学会进化路径）做了辩护。**这个问题的标准答案是"隐喻成立、字面不成立"**。
2. 只生成了一个 GFP，成功率怎么算？——两轮各 88 个设计筛出一个；论文没有给出"设计成功率"的统计口径（比如每轮多少候选进板、每板多少有活性）。这是生成蛋白论文的通病：**成功案例有说服力，成功率口径模糊**。
3. 权重只开源 ESM3-open（1.4B），98B 只给 API——科学可复现性怎么保证？——论文承认这是公司化运营的取舍；学术界只能用 API 或小模型复现核心结论。
4. 偏好对齐的数据是模型自己生成的（自举），会不会只是"自己给自己出题又自己背题"？——评测提示词（46 个配体基序）与对齐数据分布有距离，且 Pass@128 的提示词做了排列扰动，这个设计基本堵住了直接背诵的可能。

### 3.7 带着你的问题读：这一篇对你有什么用

- **链式思考 = 你模拟数据生成器的现成升级模板**。你的 mosaic generator 用 block-bootstrap 生成模拟菌群；ESM-3（或 ESM3-open，学术版免费）的"结构→序列→交替优化"流程可以生成**功能约束可指定**的模拟蛋白——造"组间功能差异已知"的模拟数据集时，功能差异不再是调参假设，而是从提示词里长出来的。
- **偏好对齐的"好/差样本对"思路可以搬到你的功效分析框架里**：把"高功效设计方案"和"低功效设计方案"当偏好对，训练一个轻量模型指导 pilot 实验设计——这是把"信息墙"问题变成一个可学习的优化问题（远期课题）。
- **esmGFP 的对照设计值得抄**：你的湿实验验证管线里，"突变关键残基后功能消失"这一招（催化残基突变对照）和转座子论文的 YXXXQ→AXXXQ 异曲同工——三篇论文不约而同用了同一个"排除替代解释"的黄金手法。

## 第 4 章 把三代串成三条主线（并接回 Evo 的学习线）

**主线一：规模即方法，而且每代都验证一次。** ESM-1b 证明 6.5 亿参数比 4000 万强；ESM-2 证明 150 亿比 6.5 亿强、还出现非线性涌现；ESM-3 证明 980 亿三模态比 70 亿强、而且**对齐收益随规模放大**（小模型对齐只加 9 个点，大模型加 39 个点）。注意和 Evo 系的对照：Evo 的 scaling laws 说的是**架构**（StripedHyena 在 DNA 上优于 Transformer），ESM 系说的是**规模**——两条腿缺一不可，合起来就是你看到的 Evo 2（40B 混合架构）。

**主线二：证据标准逐代抬升，终点永远是湿实验。** ESM-1b 的证据是基准分数；ESM-2 是时序留出的硬评测 + 百万级预测的规模展示；ESM-3 是**合成蛋白、测荧光、测光谱、做功能丧失突变对照**。这条线和你 Evo 精读里总结的"评估从跑分走向实验审计"完全同构——**记住这个规律，你判断任何一篇 AI4Bio 论文的成色就有了标尺**。

**主线三：从"表征"到"生成"，再到"对齐"。** ESM-1b/ESM-2 问"模型懂了什么"（表征）；ESM-3 问"模型能按我说的做吗"（生成+提示词）；然后问"怎么让它做得更好"（对齐）。这正是 NLP 的完整路线图在蛋白质上的复现——而 Evo 1.5（语义设计）和 Evo 2（推理时引导）在 DNA 侧同时走到了同一位置。**你现在读到的任何 2025-2026 年基模论文，几乎都能放进"表征→生成→对齐"这三个格子之一**。

## 第 5 章 新术语速查

| 术语 | 一句话人话 |
|---|---|
| 掩码语言模型（MLM） | 盖住一些位置让模型填空——双向表征的标准练法 |
| ECE（指数化交叉熵） | 用"平均犹豫几个选项"衡量的语言建模质量 |
| 伪似然 | 掩码模型算整条序列概率的近似（逐位置条件概率之积）——变异效应打分的标配 |
| t-SNE / PCA / UMAP | 高维表征的"俯瞰照"（非线性/线性降维） |
| 直接耦合分析（DCA）/ CCMpred | 从家族多序列比对里用统计模型读共进化信号的老方法 |
| UniParc / UniRef50 / UniRef90 / UniRef100 | 蛋白序列数据库的分层去重体系（数字越小去重越狠） |
| SCOPe / Pfam | 蛋白结构分类库 / 蛋白家族库 |
| HHblits | 基于 HMM 的敏感序列搜索（老牌"远源同源检测"冠军） |
| 远源同源（remote homology） | 序列看不出相似、但结构同源的蛋白关系 |
| 线性投影（linear probe） | 用最简模型（逻辑回归）从表征里"读"信息——证明信息真的在表征里 |
| 注意力 → 接触图 | 把注意力模式当成"谁挨着谁"的读出 |
| APC 校正 | 去除接触图背景耦合的标准校正 |
| TM-score / lDDT / pLDDT / RMSD | 结构相似度 / 逐残基距离分 / 预测置信度 / 结构偏差（Å） |
| Evoformer / 结构模块 | AlphaFold2 的序列处理核心 / 把表征变成三维坐标的模块 |
| 蒸馏 | 用大模型（或 AF2）的输出当训练标签 |
| 时序留出 | 测试集只用训练截止后的新数据——防"背答案" |
| CAMEO / CASP14 | 持续滚动评测的结构预测平台 / 第 14 届结构预测竞赛 |
| MGnify / 宏基因组蛋白 | 环境样本的序列库 / 培养不出来的微生物的蛋白 |
| FoldSeek | 超快结构比对搜索 |
| 离散自编码器（结构 token 化） | 把结构压成离散 token 再还原的"压缩-解压"网络 |
| 几何注意力 | 让模型直接感知原子坐标的注意力机制 |
| SS8 / SASA | 八类二级结构 / 溶剂可及表面积 |
| 生成式掩码 + 噪声调度 | 用任意掩码率训练，换来任意顺序生成 |
| 偏好优化（DPO 式） | 用好/差样本对让模型学"照着题目写答案" |
| Pass@128 | 给 128 次机会的解题率 |
| 链式思考（chain of thought） | 把大设计任务拆成多步、每步可质检的生成流程 |
| 发色团 / β 桶 / 催化三元组 | GFP 的自发光核心 / GFP 的 11 股桶状骨架 / 酶的活性中心三残基 |
| tagRFP / avGFP | 常用红色荧光蛋白（人工变体）/ 水母绿色荧光蛋白 |
| 时间校准系统发育 | 用化石等外部时间信息给进化树"对表" |

## 参考来源

- ESM-1b 全文（PubMed Central）：https://www.ncbi.nlm.nih.gov/pmc/articles/PMC8053943/
- ESM-2 全文（bioRxiv 预印本源 XML）：https://www.biorxiv.org/content/10.1101/2022.07.20.500902v1
- ESM-3 全文（bioRxiv 预印本源 XML）：https://www.biorxiv.org/content/10.1101/2024.07.01.600583v2
- ESM 代码仓库（含 ESM-1b/ESM-2 权重）：https://github.com/facebookresearch/esm
- EvolutionaryScale 仓库（ESM3-open）：https://github.com/evolutionaryscale/esm
- ESM C / ESMFold2（后续工作）：https://www.evolutionaryscale.ai/blog/esm-cambrian

*本文档与仓库中的 Evo 系列三份文档构成完整的基模学习路线：Evo 侧（DNA 基模）与 ESM 侧（蛋白基模）读完，你就同时掌握了 2024–2026 年两条主线的技术史坐标。*



